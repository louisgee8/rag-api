"""Admin CLI scripts. Run as modules from inside the container:

    docker compose exec api python -m scripts.issue_key --tenant acme

These are operator-only tools; they are NOT exposed via HTTP.
"""
